{
  "name": "ankhimate-runtimes",
  "private": true,
  "workspaces": [
    "phaser"
  ],
  "scripts": {
    "build": "npm run build --workspace @ankhimate/phaser-runtime",
    "test": "npm run test --workspace @ankhimate/phaser-runtime",
    "typecheck": "npm run typecheck --workspace @ankhimate/phaser-runtime"
  }
}
